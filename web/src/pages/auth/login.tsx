import React from "react";

interface Props {}
const Login: React.FC<Props> = ({}) => {
  return (
    <div className="login">
      <h1>Hello from Login</h1>
    </div>
  );
};

export default Login;
