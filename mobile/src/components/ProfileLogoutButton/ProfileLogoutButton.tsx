import { View, Text } from "react-native";
import React from "react";

const ProfileLogoutButton = () => {
  return (
    <View>
      <Text>ProfileLogoutButton</Text>
    </View>
  );
};

export default ProfileLogoutButton;
